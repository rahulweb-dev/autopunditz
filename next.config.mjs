/** @type {import('next').NextConfig} */
const nextConfig = {
images: {
    domains: ["images.unsplash.com","static.wixstatic.com","img.youtube.com"],
  },
  reactCompiler: true,
};

export default nextConfig;
