/** @type {import('next').NextConfig} */
const nextConfig = {
images: {
    domains: ["images.unsplash.com","static.wixstatic.com","img.youtube.com","ik.imagekit.io"],
  },
  reactCompiler: true,
};

export default nextConfig;
